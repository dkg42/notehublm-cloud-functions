node_modules/
lib/
.firebase/
firebase-debug.log
firebase-debug.*.log
.runtimeconfig.json
.DS_Store

# Environment files.
# No .env file of any kind is tracked. `.env.example` is the only committed
# template; every real environment file — including the per-project
# `.env.<projectId>` files read at deploy time — stays local. See SECURITY.md.
.env*
!.env.example

# Firestore emulator logs
firestore-debug.log
firestore-debug.*.log

# IDE
.idea/
.vscode/

# Agent tooling. `.agents/` is restored from skills-lock.json by the Firebase CLI;
# it is local scaffolding, not part of the backend.
.claude/
.agents/
